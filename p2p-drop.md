### 项目定位与架构概要

* **产品名称：** P2P Drop
* **核心定位：** 免安装、免注册、即开即用的浏览器端跨设备直连文件快传工具。
* **架构核心：**
* **前端宿主：** 静态 Web 应用（可部署于 Vercel / Cloudflare Pages / GitHub Pages）。
* **信令中介：** Supabase Realtime（负责房间配对、设备 Presence 发现、SDP/ICE 握手交换）。
* **数据传输：** WebRTC `RTCDataChannel`（局域网/公网 P2P 直连，不经服务器中转，零服务器带宽成本）。
* **公网打洞：** 公共免费 STUN 服务器（如 Google STUN）。



---

### 核心功能需求（Feature Requirements）

#### 1. 设备发现与配对模块

* **自动发现模式（局域网/同公网 IP 广播）：**
* 用户打开网页，自动进入以当前公网出口 IP（或自动生成的默认 Room）命名的频道。
* 借助 Supabase Presence，自动渲染当前频道内的在线设备列表（显示设备图标：电脑/手机、浏览器类型、随机趣味昵称）。


* **私密房间 / 提取码模式（跨公网异地传输）：**
* **房间码匹配：** 支持输入自定义房间号（如 6 位数字或单词）进入同一信令 Channel。
* **扫码/链接加入：** 支持一键生成带房间 ID 的 URL 与二维码，手机扫码即入。


* **安全授权机制：**
* 发送方发起传输前，接收方界面弹出确认浮窗（显示文件名、格式、文件大小）。
* 接收方点击“同意接收”后才正式开启 DataChannel 二进制流传输，支持一键拒绝。



#### 2. 文件传输核心引擎

* **多文件与文件夹拖拽：**
* 支持拖拽多文件、点击上传，支持大文件选择。


* **分片与流控传输（Chunking & Backpressure）：**
* 将文件切割为固定尺寸数据块（建议 16 KB – 64 KB 分片）。
* 监控 `bufferedAmount`，实施背压缓冲流控，避免高带宽导致浏览器进程爆内存或网络崩溃。


* **传输状态实时反馈：**
* 双向实时展示传输进度条（百分比、已传大小/总大小）。
* 实时传输速率（MB/s）与预计剩余时间（ETA）。
* 支持中途主动取消传输。


* **大文件内存保护与落地机制：**
* **普通小文件（<500MB）：** 内存缓冲组合为 `Blob`，自动触发浏览器下载。
* **超大文件（>1GB）：** 优先接入 **File System Access API** (`showSaveFilePicker`) 边收边写磁盘，或降级接入 Service Worker 流式落盘，避免移动端浏览器标签页 OOM 闪退。



#### 3. 连接保活与状态机管理

* **连接生命周期反馈：**
* 清晰显示连接状态节点：`正在连接信令` $\rightarrow$ `发现设备` $\rightarrow$ `NAT打洞中` $\rightarrow$ `P2P已直连` $\rightarrow$ `传输中` $\rightarrow$ `传输完成`。


* **打洞失败与网络受限提示：**
* 当双方处于对称型 NAT（Symmetric NAT）打洞失败时，明确给出友好提示（如：“当前网络属于高安全限制网络，无法直接穿透建立 P2P 直连”），而非无响应卡死。


* **断开重连：**
* 页面可见性改变（切到后台唤醒）或网络短时抖动时的自动重新订阅机制。